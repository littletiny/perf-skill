---
name: SPEAR-perf-hunter
description: Systematic Linux performance diagnosis using SPEAR (Systematic Performance Empirical Analysis & Reflection)
methodology. Use when analyzing CPU bottlenecks, high latency, resource contention, or performance regression in Linux environments - especially with Cgroup constraints, low-frequency sampling (19Hz), or complex multi-threaded applications. Requires perf script data with core/s field as input.
---

# SPEAR 性能诊断 (Systematic Performance Empirical Analysis & Reflection)

结构化性能分析方法论，通过"领域知识驱动的假设验证"实现根因定位，确保结论的可证伪性和可复现性。

## 核心原则

### 1. 领域知识优先于工具
- **建立预期先于验证**: 在启动任何工具前，先识别应用类型，建立性能预期基线
- **工具服务于假设**: 使用工具是为了"验证为什么现实不符合预期"，而非"发现热点"
- **异常驱动分析**: 只有发现"预期 vs 现实"的差距，才有深入分析的必要

### 2. 竞争性假设并行验证
- **延迟收敛**: 至少同时维护 3 条竞争性假设，避免过早下结论
- **证伪优先**: 证据应该用于证伪假设，而非证实假设
- **多因叠加**: 保留"多因素共同作用"的可能性，不强制单根因

### 3. 逻辑可溯源
- **所有结论附带证据引用**: 必须具体到工具输出、函数名、调用链
- **所有假设可证伪**: 每个假设必须有"预期指纹"和"验证方法"
- **过程可复刻**: 完整记录在 `debug/*.md` 文档中

---

## 标准工作流

### Step 1: 领域认知与预期建立

**目标**: 理解被分析对象的设计意图和应有表现

**执行内容**:
1. 识别应用类型（Web服务、数据库、参数服务器、消息队列等）
2. 激活领域知识：
   - 该类型应用的典型架构特征
   - 预期的性能表现（CPU利用率、负载分布、延迟特征）
   - 已知的常见瓶颈模式
3. 建立预期基线："正常情况下应该看到什么"
4. 对比现实观测："实际看到了什么"
5. 判定是否异常："差距是否合理"

**输出要求**: 在 debug 文档中记录预期 vs 现实的对比

---

### Step 2: 假设空间构建

**目标**: 基于领域知识和观测异常，枚举所有可能路径

**执行内容**:
1. 列出至少 3 条竞争性假设（避免搜索范围太小）
2. 每条假设必须包含：
   - 机制描述：技术原理是什么
   - 预期指纹：如果假设成立，应该观察到什么现象
   - 验证方法：用什么工具/数据可以验证
   - 证伪条件：什么证据可以否定这条假设

**示例假设格式**:
```yaml
假设A_资源竞争:
  机制: 全局锁导致串行化
  预期指纹: 热点函数包含锁操作，负载分散在多个核心
  验证: get-hotspots + find-callers
  证伪: 热点中无锁函数，或单核满载

假设B_调度压制:
  机制: 应用层主动休眠或系统调度限制
  预期指纹: 调度函数占比高，voluntary context switch 多
  验证: cluster-symbols + find-callers 已被观察到的调度相关函数
  证伪: 调度开销正常，且无主动休眠证据
```

**输出要求**: 在 debug 文档的"路径评估表"中记录所有假设

---

### Step 3: 机制评估与剪枝

**目标**: 基于领域知识评估每条假设的机制可行性，快速排除不可能路径

**业务函数名**会透传大量**领域信息**，需要根据**函数名评估技术架构设计*大胆猜想，小心举证是debug领域的的核心能力


**执行内容**:
1. 不深入代码/数据细节，仅从机制层面评估：
   - 该机制在理论上是否可能发生？
   - 是否符合该类应用的典型行为模式？
   - 是否有已知的副作用可以解释观测到的现象？
2. 机制与领域知识矛盾的假设，直接剪枝
3. 保留机制可行的假设进入下一步


**输出要求**: 在路径评估表中更新"机制评估"和"状态"

---

### Step 4: 循证深挖

**目标**: 对高置信度假设进行工具验证，收集证据

**执行内容**:
1. 为每条待验证假设设计工具验证方案
2. **并行执行**验证（不要等一个完成再做下一个）
3. 收集数据时关注：
   - 是否符合"预期指纹"？
   - 是否有意外发现（与假设矛盾或支持其他假设）？
4. 所有结论必须附带具体的工具输出引用

**工具选择原则**:
- 检查资源边界 → `check-cpu-bottleneck`
- 检查负载分布 → `show-cpu-usage` (带 `--cpu-id`)
- **分析核心级负载均衡 → `analyze-core-distribution`**
- 识别热点 → `get-hotspots --sort-by self`
- 热点溯源 → `find-callers --target <func>`
- 语义聚类 → `cluster-symbols`
- 行为模式 → `count-process-variety`
- 时序异常 → `detect-anomalies`

**关键检查点**:
- 发现调度相关函数 → 必须溯源是主动休眠还是被动抢占
- 发现负载不均衡 → 必须分析是"不能并行"还是"不想并行"
  - 使用 `analyze-core-distribution` 检查各核心利用率分布
  - 识别是单核饱和还是多核均衡
  - 检测线程休眠模式 (sleeping vs active)
- 发现锁函数 → 必须评估锁粒度和竞争范围

**输出要求**: 在 debug 文档的"深度审计记录"中记录证据

---

### Step 5: 全局审计

**目标**: 确保结论能解释所有观测到的异常

**执行内容**:
1. 对比各假设的证据强度
2. 确认结论前检查：
   - [ ] 是否解释了所有观测到的异常？
   - [ ] 是否有无法解释的孤证？
   - [ ] 是否符合领域应有表现？
   - [ ] 证据链是否闭环？
3. 如果存在无法解释的孤证，强制重置流程，更新问题定义

**决策逻辑**:
- 单因素: 只有 1 条假设证据强度高，其他都被证伪
- 多因素: 多条假设都有证据支持，需要排序优先级
- 不完整: 所有假设证据都弱，需要补充新假设

**输出要求**: 在 debug 文档的"全局审计"部分记录最终结论

---

## 文档规范

在 `debug/[问题描述].md` 维护分析过程。

### 双表结构

1. **问题演进表 (Table A)**: 记录问题定义的版本迭代和关键证据引用
2. **路径追踪与机制评估表 (Table B)**: 记录假设、机制评估、预期指纹和验证状态

### 证据引用规范

- **工具输出**: `<工具名>: <关键指标> (样本数/可靠性等级)`
- **函数引用**: `函数名` 或 `类名::方法名`
- **调用链**: `caller → callee → ...`
- **数据定位**: `CPU核心/时间戳/进程ID` 等定位信息

---

## 附录：调试文档模板

创建新的分析文档时，复制以下内容：

````markdown
# Perf Analysis: [简短的问题描述]

- **状态**: [进行中 / 已关闭 / 待审计]
- **应用类型**: [识别出的应用类型]

---

## 一、领域认知与预期 (Step 1)

**应用识别**:
- 进程名: 
- 框架线索: 
- 推断类型: 

**预期基线**:
- CPU利用率: 
- 负载分布: 
- 典型热点: 

**现实观测**:
- CPU利用率: 
- 负载分布: 
- 异常判定: 

---

## 二、假设空间 (Step 2 & 3)

| 假设 | 机制描述 | 预期指纹 | 验证方法 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| H1 | | | | |
| H2 | | | | |
| H3 | | | | |

---

## 三、循证深挖 (Step 4)

### 验证记录 1

- **目标假设**: 
- **使用工具**: 
- **关键发现**: 
- **证据引用**: 
- **对假设的影响**: [支持/证伪/无关]

---

## 四、全局审计 (Step 5)

- [ ] 是否解释了所有观测异常？
- [ ] 是否符合领域应有表现？
- [ ] 是否有无法解释的孤证？
- [ ] 证据链是否闭环？

**结论**: 

**优化建议**:

---

## 附录

### 关键工具输出

```
[在此处粘贴关键工具输出]
```
````

---

## 典型陷阱与自检

### 陷阱 1: 过早收敛
**表现**: 看到一条证据就立即下结论  
**自检**: 是否列出了至少 3 条竞争性假设？

### 陷阱 2: 忽视领域背景
**表现**: 只关注绝对数值，不问"这是否符合应有表现"  
**自检**: 是否建立了预期 vs 现实的对比？

### 陷阱 3: 工具驱动而非问题驱动
**表现**: "执行 get-hotspots 找热点"而非"验证为什么 CPU 低"  
**自检**: 每次使用工具前是否明确了要验证什么假设？

### 陷阱 4: 忽视异常信号
**表现**: 看到调度函数但不溯源，看到负载不均衡但不分析  
**自检**: 是否所有与预期不符的信号都得到了解释？

### 陷阱 5: 单因思维
**表现**: 强制找一个根因，忽视多因素叠加  
**自检**: 是否考虑了"多因素共同作用"的可能性？


---

## 附录 B: 工具参考

### 核心工具清单

| 工具 | 用途 | 典型场景 |
|------|------|---------|
| `check-cpu-bottleneck` | 检查资源限制和单核饱和 | 环境边界判定 |
| `show-cpu-usage` | 查看 CPU 利用率 (user/kernel) | 资源消耗概览 |
| `analyze-core-distribution` | 分析各核心负载分布和线程状态 | **检查负载不均衡** |
| `get-hotspots` | 识别热点函数 | 瓶颈定位 |
| `find-callers` | 热点溯源 | 调用链分析 |
| `cluster-symbols` | 按专家规则聚类 | 语义分析 |
| `count-process-variety` | 检测进程风暴 | 行为模式识别 |
| `detect-anomalies` | 检测时序异常 | 异常窗口识别 |

---

## 版本历史

### v2.1 (2026-02-28)
- **新增**: `analyze-core-distribution` 工具，支持核心级负载分析
- **修复**: `cluster-symbols --custom-rules` 支持列表格式的规则
- **文档**: 重构为规则驱动的泛化方法论

### v2.0 (Previous)
- **重构**: 移除 `--freq` 参数，直接使用 core/s 计算 CPU 利用率
- **新增**: 数据可靠性评估体系
- **模块化**: 重构为 perf_toolkit 包结构

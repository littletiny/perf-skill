# TC-06: 最终审计 - 全部完成

## 目的
验证当所有问题都完成时，`finalize` 命令允许生成报告

## 前置条件
- 有 2 completed, 1 pending 的状态（TC-04 后）

## 测试步骤

```bash
# Step 1: 先完成最后一个问题
python ../../scripts/spear.py doc complete \
  --id ISS-003 \
  --result "NORMAL: 非关键路径，优先级低"

# Step 2: 确认状态
python ../../scripts/spear.py doc list

# Step 3: 最终审计
python ../../scripts/spear.py doc finalize
```

## 预期结果

### Step 1 输出
```
✓ 已完成: ISS-003
  结果: NORMAL: 非关键路径，优先级低
```

### Step 2 输出
```
============================================================
ISSUES  STATUS  (3 completed, 0 pending)
============================================================

✅ COMPLETED
------------------------------------------------------------
ISS-001  netstat 高内核态 94.7%
         └─ 结果: LOCK_CONTENTION 38.36%, /proc/net/tcp 竞争

ISS-002  containerd-shim 高内核态 89.9%
         └─ 结果: LOCK_CONTENTION 79.84%, 单进程锁竞争远超 netstat

ISS-003  sh 高内核态 86.8%
         └─ 结果: NORMAL: 非关键路径，优先级低

============================================================
```

### Step 3 输出
```
============================================================
最终全局审计
============================================================

✅ 所有问题已处理

已完成清单:
  ISS-001  netstat 高内核态 94.7% → LOCK_CONTENTION 38.36%, /proc/net/tcp 竞争
  ISS-002  containerd-shim 高内核态 89.9% → LOCK_CONTENTION 79.84%, 单进程锁竞争远超 netstat
  ISS-003  sh 高内核态 86.8% → NORMAL: 非关键路径，优先级低

============================================================
✓ 可以生成诊断报告
============================================================
```

### 验证点
- [ ] 状态显示 "所有问题已处理"
- [ ] 列出所有已完成问题的汇总
- [ ] 明确提示 "可以生成诊断报告"
- [ ] exit code 为 0
